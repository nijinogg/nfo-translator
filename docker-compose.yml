services:
  app:
    image: starducktea/nfo-translator:latest
    ports:
      - "8099:8080"
    volumes:
      - ./data:/data
    environment:
      - PUID=1000
      - PGID=100
      - STARTUP_DELAY=360
      - DATABASE_URL=postgresql://nfo_monitor:nfo_monitor@db:5432/nfo_monitor